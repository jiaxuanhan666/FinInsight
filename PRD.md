# FinInsight 产品需求文档 (PRD)

> **版本**: v2.1  
> **更新日期**: 2026-08-01  
> **文档状态**: 迭代中  
> **产品负责人**: FinInsight Team

---

## 目录

1. [产品定位与愿景](#1-产品定位与愿景)
2. [市场背景与竞品分析](#2-市场背景与竞品分析)
3. [目标用户群体](#3-目标用户群体)
4. [核心功能架构](#4-核心功能架构)
5. [AI 技术架构详解](#5-ai-技术架构详解)
6. [数据模型设计](#6-数据模型设计)
7. [API 接口设计](#7-api-接口设计)
8. [交互设计规范](#8-交互设计规范)
9. [技术栈与部署架构](#9-技术栈与部署架构)
10. [商业模式与闭环逻辑](#10-商业模式与闭环逻辑)
11. [版本迭代路线图](#11-版本迭代路线图)
12. [风险与应对](#12-风险与应对)

---

## 1. 产品定位与愿景

### 1.1 一句话定位

**FinInsight 是一款面向 Z 世代的 AI 原生智能记账系统——不止记录流水，更通过 AI 自动识别每笔消费的"资产属性"，将传统"收支账本"升级为"消费-资产双维财务系统"。**

### 1.2 产品愿景

让每一个年轻人都能**低成本地看清自己的真实财务状况**——你的每一次消费不是终点，而可能是一笔隐藏资产的起点。

### 1.3 核心差异化价值

| 维度 | 传统记账工具 | FinInsight |
|------|------------|------------|
| 记账模式 | 手动分类，事后回顾 | AI 实时分类，记账即分析 |
| 资产视角 | 仅记录显性资产（银行/股票） | 自动发现隐性资产（潮玩/球鞋/数码/藏品） |
| 智能程度 | 规则匹配 + 饼图 | LLM 语义理解 + 时序行为分析 + 永久记忆 |
| 报告生成 | 固定模板 | AI 实时生成结构化财务报告 + 人格标签 |
| 隐私保护 | 手机号/邮箱注册 | UUID 无感注册，零隐私采集 |
| 本地能力 | 纯云端 | 支持 Ollama 本地模型，离线可用 |

### 1.4 产品调性

- **视觉**: 暗夜霓光 (Neon Noir) — 暗色底 + 毛玻璃 + 6 色霓虹光晕
- **交互**: Spring Physics 弹性动效，每次操作都有触觉反馈感
- **语言**: 专业但不冰冷，像 AI 助手在汇报工作
- **受众**: 对科技有亲近感、对财务有掌控欲望的 18-30 岁年轻人

---

## 2. 市场背景与竞品分析

### 2.1 市场洞察

- 中国 Z 世代（1995-2010 年出生）人口约 2.6 亿，月均可支配收入约 3,500 元
- 超过 60% 的 Z 世代有"潮玩/球鞋/数码"等可变现品类消费习惯
- 二手交易市场规模达万亿级（闲鱼 2024 年 GMV 超 5,000 亿），但**没有工具能自动连接"消费"与"资产"两个视图**
- 传统记账 App（随手记、挖财、鲨鱼记账）月活持续下滑，年轻用户流失严重

### 2.2 竞品矩阵

| 竞品 | 类型 | 优势 | 劣势 |
|------|------|------|------|
| **随手记** | 传统记账 | 功能全面，历史久 | UI 老旧，无 AI，学习成本高 |
| **鲨鱼记账** | 轻量记账 | 操作极简 | 无资产视图，无 AI 分析 |
| **MoneyWiz** | 专业财务 | 双复式记账法 | 价格昂贵，无中文优化 |
| **微信记账** | 自动记账 | 微信支付自动导入 | 不区分资产属性，隐私担忧 |
| **Copilot Money** | AI 记账 | UI 精美，AI 分类 | 仅限北美市场，不适用于中国 |

### 2.3 FinInsight 的蓝海空间

**"消费即资产"的认知升级** — 现有工具都把消费当作终点，FinInsight 把消费当作资产发现的起点。这是一个尚未被充分产品化的心智定位。

---

## 3. 目标用户群体

### 3.1 核心用户画像

#### 画像 A：潮玩收藏型消费者（占比 ~40%）

| 属性 | 描述 |
|------|------|
| 年龄 | 18-26 岁 |
| 职业 | 大学生 / 初入职场 |
| 月消费 | 2,000-5,000 元 |
| 消费特征 | 频繁购买盲盒/手办/球鞋/数码，有二手交易习惯 |
| 核心痛点 | "买了太多东西但不知道哪些值钱，想管理但觉得传统记账太土" |
| 使用场景 | 买完泡泡玛特 → FinInsight 自动标记为"另类资产" → 在资产台账中看到潮玩总价值 |

#### 画像 B：理性攒钱型年轻人（占比 ~35%）

| 属性 | 描述 |
|------|------|
| 年龄 | 22-30 岁 |
| 职业 | 职场 2-5 年 |
| 月消费 | 3,000-8,000 元 |
| 消费特征 | 有大宗消费目标（买房/旅行/进修），关注储蓄率 |
| 核心痛点 | "想攒钱但没有追踪工具，传统理财 App 太复杂" |
| 使用场景 | 设置攒钱目标 → AI 分析储蓄率 → 报告给出优化建议 |

#### 画像 C：副业/自由职业者（占比 ~25%）

| 属性 | 描述 |
|------|------|
| 年龄 | 20-30 岁 |
| 职业 | 自由职业 / 内容创作者 / 二手交易者 |
| 月消费 | 不固定 |
| 消费特征 | 收入来源多样，支出中有部分可算"投资" |
| 核心痛点 | "分不清花出去的钱哪些是消费哪些是投资" |
| 使用场景 | 买设备/器材 → AI 识别为双属性品类 → 用户确认后归入资产 |

### 3.2 非目标用户

- 50 岁以上传统记账用户（需求不同，习惯不同）
- 企业财务管理人员（需要复式记账、审计追踪等企业级功能）
- 纯投资用户（FinInsight 不做投资建议、不预测涨跌）

---

## 4. 核心功能架构

### 4.1 功能模块全景图

```
                         FinInsight v2.1
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
   ┌────┴────┐          ┌────┴────┐          ┌────┴────┐
   │ 智能记账  │          │ 资产管理  │          │ AI 分析  │
   │ 流水线    │          │ 台账      │          │ 引擎     │
   └────┬────┘          └────┬────┘          └────┬────┘
        │                     │                     │
   ┌────┴──────────┐   ┌─────┴──────┐   ┌─────────┴──────┐
   │· 金额+品类录入 │   │· 另类资产   │   │· 四层分类决策   │
   │· AI 自动分类   │   │· 金融资产   │   │· AI 财务报告    │
   │· 用户确认/纠偏 │   │· 负债台账   │   │· 财务人格标签   │
   │· 自定义品类    │   │· 月度还款   │   │· 永久记忆学习   │
   │· 支付方式标记  │   │· 资产结构图 │   │· 分类准确率统计  │
   └───────────────┘   └────────────┘   └────────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
         ┌────┴────┐    ┌────┴────┐    ┌────┴────┐
         │ 总览仪表 │    │ 攒钱计划 │    │ 个人中心 │
         │ 盘       │    │         │    │         │
         └────┬────┘    └────┬────┘    └────┬────┘
              │               │               │
         ┌────┴──────┐  ┌────┴──────┐  ┌────┴──────┐
         │· 净资产卡片│  │· 目标创建  │  │· 用户信息   │
         │· 收支四维  │  │· 进度追踪  │  │· 数据概览   │
         │· 月度还款  │  │· 环形进度图│  │· 账本合并   │
         │· AI 分类   │  │· 存钱记录  │  │· 版本信息   │
         │· 收支图表  │  │· 达成庆祝  │  │· 隐私承诺   │
         └───────────┘  └───────────┘  └───────────┘
```

### 4.2 用户核心流程

```
首次使用:
  打开 App → 引导页 (3屏功能介绍) → 点击"开始使用"
  → UUID 自动生成 → 进入总览页 (空状态)

日常记账:
  底部 Tab "记账" → 输入金额 → 选择品类标签 → 点击"记录"
  → AI 实时分类 → 如果是双属性品类 → 弹出确认弹窗 → 用户选择自用/可卖
  → 自动更新资产台账 → 回到总览页看到数据变化

AI 报告:
  底部 Tab "AI报告" → 选择周期 (周/月/年) → 点击"生成报告"
  → 服务端聚合数据 → 注入 AI 模型 → 返回结构化报告 JSON → 前端渲染

月度还款:
  总览页自动同步资产台账中的负债月供 → 每月1日重置状态
  → 用户勾选"已还" → 进度条更新 → 自动追踪本月还款进度
```

---

## 5. AI 技术架构详解

### 5.1 设计哲学

**"AI 不是产品的插件，而是产品的引擎。"**

FinInsight 的 AI 系统不是在传统记账上叠加的 chatbot 功能，而是从底层数据结构到上层交互体验都以 AI 决策为核心设计的。每一笔交易的写入、每一次资产的归类、每一份报告的生成，都流经 AI 决策流水线。

### 5.2 四层 AI 分类决策流水线

```
┌──────────────────────────────────────────────────────────────┐
│                    AI 分类决策流水线                           │
│                                                              │
│  输入: { categoryNote, type, amount, historyNotes }          │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ Layer 1: LLM 语义理解 (DeepSeek Chat API)            │     │
│  │ ─────────────────────────────────────────────────    │     │
│  │ · 模型: deepseek-chat                               │     │
│  │ · Temperature: 0.1 (追求确定性)                      │     │
│  │ · Max Tokens: 200                                   │     │
│  │ · System Prompt: 消费分类专家角色 + 严格约束          │     │
│  │ · Few-shot: 10 组典型样例 (盲盒→dual, 午餐→pure...)    │     │
│  │ · 输出: JSON {category, confidence, reasoning}      │     │
│  │ · 失败 → 自动 fallback 到 Layer 2                    │     │
│  └─────────────────────────────────────────────────────┘     │
│                           ↓ fallback                          │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ Layer 2: 本地模型兜底 (Ollama + qwen2.5:7b)          │     │
│  │ ─────────────────────────────────────────────────    │     │
│  │ · 模型: qwen2.5:7b (可替换)                          │     │
│  │ · 先检查可用性: GET /api/tags                        │     │
│  │ · 完全离线运行，数据不出设备                          │     │
│  │ · 失败 → 自动 fallback 到 Layer 3                    │     │
│  └─────────────────────────────────────────────────────┘     │
│                           ↓ fallback                          │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ Layer 3: 规则引擎 (关键词匹配)                        │     │
│  │ ─────────────────────────────────────────────────    │     │
│  │ · PURE_CONSUMPTION: 50+ 关键词 (餐饮/出行/日用...)    │     │
│  │ · DUAL_ATTRIBUTE:   30+ 关键词 (盲盒/球鞋/数码...)    │     │
│  │ · ASSET_REALIZATION: 7 关键词 (卖掉/变现/回血...)     │     │
│  │ · 100% 确定性，0 延迟，永远可用                       │     │
│  └─────────────────────────────────────────────────────┘     │
│                           ↓                                   │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ Layer 4: 时序行为分析 (Trade Pattern Detection)      │     │
│  │ ─────────────────────────────────────────────────    │     │
│  │ · 算法: 字符集 Jaccard 相似度                         │     │
│  │ · 相似度阈值: 0.4                                    │     │
│  │ · 模式识别:                                          │     │
│  │   - 有买有卖 → asset_trading (小众资产交易)          │     │
│  │   - 只买不卖 → pending (待用户确认)                  │     │
│  │   - 只卖不买 → asset_realization (资产变现)          │     │
│  └─────────────────────────────────────────────────────┘     │
│                           ↓                                   │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ 持久记忆层: persistentTags                            │     │
│  │ ─────────────────────────────────────────────────    │     │
│  │ · 高置信度 (>0.85) → 自动记忆                         │     │
│  │ · 用户确认 → 永久记忆 (source: user_confirmed)       │     │
│  │ · 后续同品类 → 跳过 AI，直接使用记忆标签              │     │
│  │ · 效果: 冷启动 50% 准确率 → 1 周后 85%+ → 1 月后 95%+ │     │
│  └─────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

### 5.3 AI 分类 Prompt 设计

**System Prompt 核心原则:**

1. **角色约束**: "专业的青年个人财务分析助手，专注于 18-30 岁年轻用户"
2. **严格边界**: "仅判定资金使用属性，不研判物品价值、不预测涨跌、不做投资建议"
3. **安全护栏**: 用 JSON Schema 强约束输出格式，正则提取 + JSON.parse 解析
4. **Few-shot 引导**: 10 组精心挑选的典型案例覆盖盲盒(潮玩)/球鞋/数码/奢侈品/餐饮/日用品/二手变现等场景

### 5.4 AI 报告生成流程

```
聚合查询 (服务端)
  ├── 周期交易数据 (按 period 筛选)
  ├── 资产/负债数据 (全量)
  ├── 上一周期对比数据 (环比计算)
  ├── AI 分类统计 (分类次数/置信度/准确率)
  └── 消费排名数据 (TOP8 品类)

        ↓ 注入为 User Message

LLM 生成 (DeepSeek / Ollama)
  ├── System Prompt: 报告结构定义 + 人格标签库
  ├── Temperature: 0.4 (平衡创造性与准确性)
  └── 输出: 结构化 JSON (6 大模块)

        ↓ JSON 解析 + 落库

前端渲染
  ├── 消费结构洞察
  ├── 资产变动快照
  ├── 财务健康指标 (储蓄率/负债率/流动性比率)
  ├── AI 建议 (2 条行为观察)
  └── 财务人格标签 (6 种人格: 收藏家/攒钱达人/均衡型...)
```

### 5.5 AI 能力的边界与安全

| 原则 | 实施方式 |
|------|---------|
| **不预测市场** | System Prompt 明确禁止研判物品价值、预测涨跌 |
| **不推荐投资** | 所有建议均为"行为观察"，不涉及具体投资标的 |
| **数据不出境** | 支持 Ollama 本地模型，数据完全留存在用户设备 |
| **用户可控** | 所有 AI 分类结果可由用户手动纠正 (userOverride) |
| **降级可用** | 即使所有 AI 服务不可用，规则引擎确保基础功能正常运行 |

---

## 6. 数据模型设计

### 6.1 核心实体关系

```
┌──────────┐    1:N    ┌──────────────┐    1:N    ┌───────────────┐
│  users   │──────────→│ transactions │←──────────│persistentTags │
│  (UUID)  │           │              │           │ (品类标签记忆)  │
└────┬─────┘           └──────┬───────┘           └───────────────┘
     │                        │
     │ 1:N                    │ 1:N (自动归集)
     │                        │
     ├────────────────┐       ↓
     │                │  ┌──────────┐
     ▼                │  │  assets  │ (AI 自动创建或用户手动)
┌──────────┐          │  └──────────┘
│liabilities│         │
└──────────┘          │
     │                │
     ├────────────────┤
     │                │
     ▼                ▼
┌──────────────┐  ┌──────────────┐
│savingsGoals  │  │ aiReports    │
│(攒钱目标)     │  │ (AI 报告)     │
└──────────────┘  └──────────────┘
```

### 6.2 核心表结构

#### users
```
{
  uuid: string          // PK: UUID v4
  nickname: string      // 用户昵称
  createdAt: number     // 注册时间戳
  lastActiveAt: number  // 最后活跃时间
  mergedFrom?: string   // 账本合并来源 UUID
}
```

#### transactions
```
{
  id: string            // PK: UUID v4
  userUuid: string      // FK → users
  amount: number        // 金额
  baseAmount: number    // 基准金额 (CNY)
  currency: string      // 币种
  type: 'income' | 'expense'
  categoryNote: string  // 品类备注 (用户输入)
  note: string          // 附加备注
  timestamp: number     // 交易时间
  aiCategory: string    // AI 分类结果
  aiConfidence: number  // AI 置信度 (0-1)
  aiReasoning: string   // AI 判定理由
  userOverride: boolean // 用户是否手动纠正
  persistentTagId: string // FK → persistentTags
  paymentMethod: string // 支付方式
}
```

#### persistentTags
```
{
  id: string            // PK
  userUuid: string      // FK → users
  keyword: string       // 品类关键词
  category: string      // AI 判定类别
  source: string        // 'ai_detected' | 'user_confirmed'
  createdAt: number
}
```

#### assets / liabilities / savingsGoals / aiReports

完整表结构参见 [server/src/db/index.ts](server/src/db/index.ts)。

### 6.3 存储策略

- **生产环境**: 单 JSON 文件持久化 (`server/data/fininsight.json`)
- **持久化机制**: 300ms 防抖批量写入，避免频繁 I/O
- **内存加速**: 启动时全量加载到内存 Map，读操作 O(1)
- **客户端缓存**: IndexedDB (Dexie) 离线缓存最近交易数据
- **扩展预留**: 架构支持切换至 SQLite/PostgreSQL，仅需替换 db/index.ts 的实现

---

## 7. API 接口设计

### 7.1 设计原则

- **RESTful 风格**, JSON 请求/响应
- **无感认证**: `X-User-UUID` Header，UUID v4 格式校验
- **错误码体系**: 标准 HTTP Status Code + 业务错误码
- **幂等性**: 60 秒重复检测，防止重复提交

### 7.2 接口清单

| Method | Path | Auth | 说明 |
|--------|------|------|------|
| `POST` | `/api/user/register` | No | 用户注册/激活 |
| `GET` | `/api/user/profile` | Yes | 获取用户信息 |
| `POST` | `/api/user/merge` | Yes | 账本合并迁移 |
| `GET` | `/api/transaction` | Yes | 交易列表 (分页+周期筛选) |
| `POST` | `/api/transaction` | Yes | 创建交易 (触发 AI 分类) |
| `PUT` | `/api/transaction/:id` | Yes | 更新交易 (用户纠偏) |
| `DELETE` | `/api/transaction/:id` | Yes | 删除交易 |
| `GET` | `/api/transaction/summary` | Yes | 月度收支摘要 |
| `GET/POST/PUT/DELETE` | `/api/asset` | Yes | 资产 CRUD |
| `GET/POST/PUT/DELETE` | `/api/liability` | Yes | 负债 CRUD |
| `GET/POST/PUT/DELETE` | `/api/savings` | Yes | 攒钱目标 CRUD |
| `GET` | `/api/report` | Yes | 报告列表 |
| `POST` | `/api/report/generate` | Yes | 生成 AI 报告 |
| `GET` | `/api/report/:id` | Yes | 报告详情 |
| `GET` | `/api/report/accuracy` | Yes | AI 分类准确率统计 |
| `POST` | `/api/analytics/track` | No | 前端埋点 (sendBeacon) |
| `GET` | `/api/analytics/dashboard` | Yes | 管理后台数据 |

---

## 8. 交互设计规范

### 8.1 设计系统：暗夜霓光 (Neon Noir)

```
色彩系统:
  背景层:  #09090f (deep) → #12121f (primary) → #1a1a2e (elevated)
  霓虹层:  #a78bfa (purple) #34d399 (mint) #f472b6 (pink)
           #60a5fa (blue)   #fbbf24 (amber) #fb7185 (coral)
  文字层:  #f1f1f6 (primary) #a1a1aa (secondary) #52525b (muted)

边框系统:
  subtle: rgba(255,255,255,0.06)  → default: 0.08 → active: 0.30

动效系统:
  ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1)
  dur-fast: 150ms / dur-normal: 300ms / dur-slow: 500ms

字体系统:
  display: Lexend (Google Fonts)  — 数字/标题专用
  body: -apple-system, PingFang SC, Microsoft YaHei — 正文专用
```

### 8.2 关键交互模式

| 场景 | 交互方式 | 反馈 |
|------|---------|------|
| 记账提交 | 点击"记录"按钮 | AI 分类 → Toast 提示结果 / 弹窗确认 |
| 品类选择 | 点击 chip 标签 | 标签高亮 + 自动填入输入框 |
| 自定义品类 | 点击"+ 自定义" → 输入名称 → 回车 | 新标签出现在列表末尾 (紫色虚线边框) |
| 月度还款 | 点击圆形复选框 | 即时切换 ☐/☑ + 进度条动画 |
| AI 报告 | 选择周期 → 点击生成 | Loading 态 → 报告卡片动画入场 |
| 所有卡片 | hover | 边框变亮 + 上移 1-2px + 紫色光晕 |

### 8.3 移动端适配

- 最大宽度 480px，居中布局 (PWA-like 体验)
- 底部安全区域适配: `env(safe-area-inset-bottom)`
- 底部导航 5 Tab，fixed 定位 + 毛玻璃模糊背景
- 所有点击区域最小 40×40px (符合 Fitts' Law)

---

## 9. 技术栈与部署架构

### 9.1 技术选型理由

| 技术 | 选型理由 |
|------|---------|
| **Vue 3 + Composition API** | 响应式系统天然适配财务数据实时更新，`<script setup>` 降低样板代码 |
| **Hono** | 比 Express 快 3-5 倍，原生 TypeScript 支持，边缘部署友好 |
| **JSON File DB** | 零依赖、零配置、单文件备份，适合个人/小团队产品起步阶段 |
| **DeepSeek API** | 中文语义理解能力业界领先，价格仅为 GPT-4 的 1/50 |
| **Ollama** | 支持完全离线 AI，满足隐私敏感用户需求 |
| **Vercel + Railway** | 前端静态托管免费，后端按需付费，总运维成本约 $5/月 |

### 9.2 部署拓扑

```
                    Internet
                        │
            ┌───────────┴───────────┐
            │                       │
     ┌──────┴──────┐        ┌──────┴──────┐
     │   Vercel    │        │   Railway   │
     │   (CDN)     │        │   (Node.js) │
     │             │  API   │             │
     │ client/dist ├───────→│ server/     │
     │ (静态 SPA)  │        │ Port 3000   │
     └─────────────┘        └──────┬──────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
            ┌─────────────┐ ┌──────────┐ ┌──────────┐
            │ DeepSeek API │ │  Ollama  │ │ JSON DB  │
            │ (云端 LLM)   │ │ (本地模型)│ │ (数据文件)│
            └─────────────┘ └──────────┘ └──────────┘
```

### 9.3 运行成本估算

| 项目 | 月成本 | 说明 |
|------|--------|------|
| Vercel 静态托管 | $0 | Hobby Plan (100GB 带宽) |
| Railway 服务端 | $5 | Starter Plan (512MB RAM) |
| DeepSeek API | $2-10 | 按量计费，约 ¥0.001/1K tokens |
| Ollama | $0 | 本地运行，零成本 |
| 域名 (可选) | $1 | .com 域名年均 $12 |
| **合计** | **$8-16/月** | 支撑 1,000-5,000 DAU |

---

## 10. 商业模式与闭环逻辑

### 10.1 当前阶段 (v2.x): 完全免费 + 数据积累

- **目标**: 获取种子用户，验证 AI 分类准确率，积累品类标签数据
- **策略**: 开源 + 免费自部署 + 本地 AI 支持
- **核心指标**: AI 分类准确率 > 85%，用户 7 日留存 > 30%

### 10.2 v3.0+ 商业闭环设计

```
┌─────────────────────────────────────────────────────┐
│                  商业闭环路径                         │
│                                                     │
│  Free Tier                 Pro Tier ($3/月)         │
│  ─────────                 ─────────────            │
│  · 基础 AI 分类            · 全部 Free 功能 +        │
│  · 规则引擎兜底            · 优先 DeepSeek 推理       │
│  · 月报                    · 周报 + 年报             │
│  · 1 个攒钱目标            · 无限攒钱目标            │
│  · 本地 Ollama             · 自定义 AI 报告模板      │
│                            · 导出 CSV/PDF            │
│                            · 无广告                  │
│                                                     │
│  增长飞轮:                                           │
│  更多用户 → 更多交易数据 → 更强的 persistentTags     │
│  → 更高的 AI 分类准确率 → 更好的用户体验              │
│  → 更高的留存率 → 更多的付费转化                      │
└─────────────────────────────────────────────────────┘
```

### 10.3 低成本商业闭环

核心思路：**用 AI 能力做差异化、用开源做信任、用自托管做低成本**。

- **AI 成本极低**: DeepSeek API 每千 token ¥0.001，单用户月均 API 调用约 30-60 次消费分类 + 1-2 次报告生成，成本 < ¥0.05/用户/月
- **运维成本极低**: JSON File DB 无需数据库服务器，Railway 免费额度覆盖 500 小时/月
- **获客成本低**: 开源社区自然流量 + 小红书/B站 产品体验分享
- **产品即壁垒**: persistentTags 品类记忆网络效应——用户越多，AI 越准，迁移成本越高

---

## 11. 版本迭代路线图

### v2.0 (已完成)
- ✅ AI 四层分类决策流水线
- ✅ 资产负债双台账
- ✅ AI 财务报告自动生成
- ✅ Ollama 本地模型支持
- ✅ UUID 无感注册 + 账本合并
- ✅ 暗夜霓光设计系统

### v2.1 (当前版本 — 2026.08)
- ✅ 启动页 3 屏功能引导
- ✅ 月度还款任务追踪
- ✅ 用户自定义品类系统
- ✅ 导航架构优化 (攒钱回归底部 Tab / 我的迁移至左上角)
- ✅ 个人中心数据概览升级

### v2.2 (规划中 — 2026.09)
- 🔲 多币种支持 + 实时汇率
- 🔲 预算管理模块 (月度预算 + 超支预警)
- 🔲 交易记录搜索 + 高级筛选
- 🔲 数据导出 CSV
- 🔲 AI 分类准确率看板 (用户可见)

### v3.0 (规划中 — 2026.Q4)
- 🔲 多设备同步 (WebSocket 实时同步)
- 🔲 账单 OCR 识别 (拍照记账)
- 🔲 语音记账 (ASR → NLP → 自动填表)
- 🔲 社区财商内容 (基于人格标签的内容推荐)
- 🔲 Pro 订阅体系上线

---

## 12. 风险与应对

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| DeepSeek API 服务中断 | AI 分类降级为规则引擎 | 低 | 三层 fallback 自动切换，用户无感 |
| 用户数据丢失 | 用户流失 | 中 | JSON 文件每日自动备份 + 客户端 IndexedDB 缓存 |
| AI 分类误差导致用户不满 | 信任度下降 | 中 | 所有 AI 结果可手动纠正 + 准确率透明展示 |
| 竞品快速跟进 AI 记账 | 差异化缩小 | 高 | 持续积累 persistentTags 网络效应 + 快速迭代 |
| JSON DB 性能瓶颈 (>10万条) | 响应变慢 | 低 | 预留 SQLite 迁移路径，1 天内可完成切换 |

---

## 附录 A: AI Prompt 模板

完整 Prompt 参见:
- [server/src/ai/prompts/classify.ts](server/src/ai/prompts/classify.ts) — 消费分类 Prompt + 10 组 Few-shot
- [server/src/ai/prompts/report.ts](server/src/ai/prompts/report.ts) — 财务报告 Prompt + 6 种人格标签库

## 附录 B: 关键代码文件索引

| 文件 | 说明 |
|------|------|
| [server/src/ai/classifier.ts](server/src/ai/classifier.ts) | 四层 AI 分类决策流水线核心逻辑 |
| [server/src/routes/transaction.ts](server/src/routes/transaction.ts) | 交易 API → AI 分类触发入口 |
| [server/src/routes/report.ts](server/src/routes/report.ts) | AI 报告生成完整流程 |
| [client/src/views/HomeView.vue](client/src/views/HomeView.vue) | 总览仪表盘 (含月度还款追踪) |
| [client/src/views/RecordView.vue](client/src/views/RecordView.vue) | 记账表单 (含自定义品类) |
| [client/src/styles/variables.css](client/src/styles/variables.css) | 暗夜霓光设计系统 CSS 变量 |

---

> **FinInsight** — *AI is not the feature. It's the foundation.*
